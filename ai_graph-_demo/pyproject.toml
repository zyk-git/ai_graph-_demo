[project]
name = "ai-graph-demo-scripts"
version = "0.0.0"
requires-python = ">=3.10"
description = "Python helpers (LLM + Chroma) for dish corpus"
dependencies = [
    "openai>=1.40.0",
    "chromadb>=0.5.0",
    "python-dotenv>=1.0.0",
    "faiss-cpu>=1.12.0",
]

[tool.uv]
package = false
