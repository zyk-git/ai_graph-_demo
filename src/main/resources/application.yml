spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dishs?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key:  sk-a5316e22df264a10aa431e3a2428ec5e
      chat:
        options:
          model: qwen-max-latest
      embedding:
        options:
          model: text-embedding-v1

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG