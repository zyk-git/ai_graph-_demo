spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dishes?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key:  sk-a5316e22df264a10aa431e3a2428ec5e
      chat:
        options:
          model: qwen-max
      embedding:
        options:
          model: text-embedding-v1

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# RAG：本地菜品语料（每行一条，格式见 DishRagService）。优先读该路径；不存在则尝试 classpath:dish_corpus.txt
app:
  rag:
    dish-corpus-path: dish_corpus.txt
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG